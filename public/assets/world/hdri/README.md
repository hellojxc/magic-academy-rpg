Project HDRI environment maps live here.

`npm run assets:world:materials` generates:

- `academy-night-atrium.hdr`
- `academy-night-atrium.preview.png`

The `.hdr` file is a Radiance RGBE equirectangular environment used by the R3F runtime for image-based lighting. The `.preview.png` file is only a tone-mapped review image.
