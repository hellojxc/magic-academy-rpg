# Player Source Model

Place the editable production source model here:

```text
assets/characters/player/source/player.blend
```

Export it with:

```sh
npm run assets:characters:source:export -- --character player
```
