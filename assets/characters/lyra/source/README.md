# Lyra Source Model

Place the editable production source model here:

```text
assets/characters/lyra/source/lyra.blend
```

Export it with:

```sh
npm run assets:characters:source:export -- --character lyra
```
